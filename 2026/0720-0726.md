# 学び

## 7/20（月）

- 2進数の引き算で計算プロセスにおいて2が突如登場していたが、これを10進数のときの計算と同じように考えたら、引くことができないときは隣の位から借りてくるという構造が同じであることがわかった
- 2のべき乗チェックに `&` を使っている意味が最初理解できなかったが、2のべき乗には2進数に直したとき、必ず "1" がどこか一箇所だけに現れ、残りはすべて "0" になる、という性質をつかっている。裏を返せば、2のべき乗ではない数字は "1" が2ヶ所以上存在することになる。
- 1を引くことで（x - 1）、「ON（1）になっている電球のうち『一番右にあるもの』を探して消し（0にし）、それより右にある OFF（0）の電球をすべて ON（1）にする」という動きになる（10進数の引き算のときと動き方が同じ）というルールを知った
- この判定の本当の目的は「1が1個だけしか存在しない数字かどうか？」を確かめること
- 「1を1個消す引き算」と「重ね合わせる`&`演算」を組み合わせることで、「元々1が1個しか存在しなかったか」をたった1行で暴いている、というのがこの式のカラクリ
- `(x+1)`: 一番右端の "0" を "1" にし、それより右の "1" を "0" にする(`x-1`の逆)。「右端から連続する 1」の範囲を拡大することに使用できる。これは、あくまで「最右端の "0" を "1" に塗り替えた後の、新しい数値を取得することに使える。
- ビットを反転させたのになぜマイナスがついた数字になるのは、コンピュータ内部でマイナスの数を表現するときに2の補数（2's complement）という仕組みを使っているから
- 2の補数: マイナスの数を、ビット（0と1）だけで表現するためのルール。コンピュータの回路にはマイナス（−）という記号が存在しないため、プラスの数もマイナスの数も、すべて「0と1の並び」だけで表す必要がある
- 引き算を足し算の回路で実現するために、一見面倒な式になる
- 最上位ビット（一番左のビット）が "0" ならプラス、"1" ならマイナスというルールは、2の補数表現を使っている限り、桁数に関わらず共通の絶対ルール。この一番左のビットのことを、符号ビット（Sign Bit） と呼ぶ
- 同じ数字でも解釈のルールを後から適用することで、そのビット数を符号なしとして扱うか、符号ありとして扱うかを決めることができる（符号なし整数: Unsigned・符号あり整数: Signed／2の補数）。符号ありの場合、一番左端の数字が "1" の場合はマイナス、"0" の場合はプラスとして扱う。
- ビットを反転させたものと元の数を足すと `-1`（全ビット1）になる

```md
1. x + ~x は必ず全ビットが 1（= -1）になる
2. → x + ~x = -1 
3. ~x を固定して移項すると ~x = -(x + 1) になる
```

- 特定の部分だけビット反転（0と1を入れ替え）したいときは、「反転させたい桁に "1" を立てたマスク数値を用意して XOR（`^`）をとる方法がある（1 と XOR をとると、元のビットが確実に裏返る）
- NOT演算子 `~`: メモリ上のすべての桁（全ビット）を一括で反転する
- XOR演算子 `^`: ひっくり返したい桁だけをピンポイントで反転できる

```python
print(bin(~4 & 7))
print(bin(~143 & 255))
```

- この書き方が回りくどく感じ、なぜ必要なのかわからなかったが、これはPythonの仕様により桁数が無限に伸びることを回避するための方法だった。C言語やJavaなどは「8ビット」や「32ビット」とデータの枠（桁数）が決まっているのに対して、Pythonは無限に伸びるらしい。そのため、`& 7`（3ビット分（7 = 111）だけ切り出す） や `& 255`（8ビット分（255 = 1111 1111）だけ切り出す） を使い、上の無限の 1 を無理やり削ぎ落とすようにしている。
- ビットシフト: ビット列（2進数）を左右にずらす（shiftさせる）操作
- `<<`: 左ビットシフト（Left Shift）
- `>>`: 右ビットシフト（Right Shift）
- 書き方: `動かしたい数値 >> 右へシフト（移動）させる回数（桁数）`
- 特定桁を1にする(ON): `number | (1 << i)`（ORを使う）
- 特定桁を0にする(OFF): `number & ~(1 << i)`（NOTで作った型紙とANDをとる）

## 7/21（火）

### 自学

- $x \oplus 0$ ＝ 「元のビットを反転させず、そのまま通過させる（素通り）」
- $x \oplus x$ ＝ 「自分自身と比較して、一致した場所を全部消し去る（全消去）」
- .mdで↑の書き方を知った
- XORを使った判定が難しく感じ、思った以上に前進できなかった

### 業務

- 先週Redshiftに作成したテーブルに指定したユーザーグループに権限付与まで行ない、問題なく動いていると思ったが、実際にはグループメンバーに対して権限が付与されていないことが発覚
- 原因は、GRANT文の後に `USAGE` を付けていなかったことが原因だった（スキーマに対して `GRANT USAGE` も必要だったということ）
- このクエリのテーブル名の部分を変更して実行してという依頼があったので素直に行なってしまったが、必ずクエリの内容を確認してから実行する、内容を疑う視点を持って取り組む必要があると学ぶことができた。今回は、テストデータの格納までを他ユーザーが実施するという内容だったので、本番には影響がなかったが、今後は気を付けていく
- これに付随して、テーブル権限があるかどうかやテーブルオーナーの確認の仕方を知ることができた
- Redshift（PostgreSQL系）の権限システムは2階層のセキュリティになっており、スキーマ権限とテーブル権限がある
- スキーマ権限: 特定のスキーマに入り、中にどんなオブジェクトがあるかを見るための権限
- テーブル権限: 特定のテーブルの中身を読み書きするための権限
- `Ctrl + Enter`: 1つのSQL文（セミコロン `;` で区切られた1ブロック）」だけを実行する。複数行を範囲選択していても、先頭の1文目だけを実行して終わってしまうか、カーソル位置の構文しか評価されて終わる
- `Alt + X`: ファイル内（または選択範囲内）のすべてのSQL文を上から順番に実行する
- データベースの権限周りは一度しっかり整理した方がよさそうだ。今回のことがきっかけで、権限によってクエリを使い分けるためにも身に着けよう
- 権限: システム権限・オブジェクト権限・スキーマ権限・カラム権限／フィールド権限
- データベースにおいて、テーブルのオーナー（所有者）になることで、テーブル定義の変更（ALTER TABLE）やテーブルの削除（DROP TABLE）などの完全な管理権限を得ることができる。そのため、グループに属するユーザーがテーブル定義を変更できるようにするためには、所有者自体をグループに変更するか、テーブルの所有者であるユーザー（または専用ロール）の権限を、対象のユーザーに付与する必要がある（直接変更 or 権限継承）
- `ALTER TABLE` や `DROP TABLE` などの管理操作は「オブジェクト権限（GRANT）」の対象外であり、オーナーシップ（またはスーパーユーザー権限）でのみ制御される 
- システムカタログ: データベースの構成情報を管理するテーブルおよびビューの一群（仕組み全体） 
- ユーザー認証の時には、DBMSは自身のシステムカタログを参照して、ユーザーが存在するかどうかを確かめる
- クエリ → システムカタログ確認 → アクセス権限の判定（実行許可 or エラー）

## 7/22（水）

### 自学

- XORをつかって「欠けている数字」「重複している数字」をそれぞれ探す方法
- 欠けている数字を探す場合: 重複して消えるはずのペアから 1つ足りない ので、理想側に残った数字が浮き出る
- 重複している数字を探す場合: 理想より 1つ多い（計3個になる） ので、ペアで消しきれなかった1つが浮き出る

### 業務
- 月次処理のファイル集約と基礎クレンジング、必要データの抽出を自動化するスクリプトを作成中、ジャンルの一覧表であるマスタとデータフレーム内のジャンル列が一部変換できない課題が発生
- ジャンルのマスタファイル・元ファイルをそれぞれデータフレーム化した後、元ファイルの内容をマスタファイルの変換後カラムの内容に統一したかったが、一部だけ変換できなかった
- そこで、それぞれのデータフレームの「ジャンル」カラムだけ文字列化して、キーをそろえる操作を加えたが、それでも解決できず
- 原因は、分類コードのマスタに変換前後の関係が記述されていなかったこと、加えて、数年間マスタの更新がなく、最新の内容が反映されていなかった
- Sparkを使ってRedshiftの対象テーブルから必要カラムのデータだけを引っ張り、それをマスタとして使えるように改修
- もともとはExcelファイルで管理されていて、継ぎ足し継ぎ足しで人の手によって入力されていたようなので、スクリプト実行によって最新のマスタを自動作成し、そのマスタを読み込む仕様に変更した（ヒューマンエラーの防止）
- 分類コードの参照は全社共有よりもRedshift内で管理している商品テーブルのコードの方が正確という事実
- `SELECT DISTINCT` は、「指定したすべての列の組み合わせ」 で重複を除外するため、`DISTINCT` の後に何個の列を指定するかで挙動が変わる
- /etc/ 直下のライブラリが読み取れない問題
- OSのパスとPythonの検索パスを混同して理解していたことが原因
```md
### OSのパス（PATH）
- 探す対象: 実行可能なコマンド（プログラム）
- ターミナルで `python3` や `ls` と打ったとき、OSがどのフォルダにあるプログラムを実行すべきかを探す場所
- ライブラリは直接実行するプログラムではないため、ここに登録しても Python は認識できない

### Pythonの検索パス（sys.path）
- 探す対象: `import` で呼び出すライブラリ（Pythonモジュール）
- Python内部で `import forge` と書いたとき、Pythonがどのフォルダの中に forge があるかを探すリスト
```
- export PYTHONPATH="${PYTHONPATH}:/etc" と設定したことで、できた流れ
1. OSの環境変数 `PYTHONPATH` に `/etc` を追加
2. Python が起動する際、この `PYTHONPATH` の値を読み取り、sys.path（ライブラリ検索リスト）の先頭付近に `/etc` を自動で追加
3. `import forge` を実行した時に Python が `/etc/forge` を見つけられるようになった
- OS側で PYTHONPATH を通すことで、Python起動時に sys.path へその場所が自動登録され、import が通るようになるという流れ
- 新規プロジェクトのためのIT管轄だったテーブルを自分が課で使用するために所属課仕様のテーブルに再定義する機会をいただいた。期限は来週の実質水曜日までなので明日からテーブル定義書とテーブルを確認しながら進めていく