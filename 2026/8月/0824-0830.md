## 8/24(月)

- ファイル自体の更新日時の取得方法を知った
- ファイルを開いて中身を読む（Readする）ことなく、OSから高速にこれらの属性情報だけを引っ張ってくる操作を「stat する」と表現する
- stat とは、「ファイルの中身ではなく、ファイル自体に関する属性情報（メタデータ）」 のこと 
- `stat().st_mtime` が返すのは、UNIXタイムスタンプ（POSIXタイムスタンプ）と呼ばれる数値（1970年1月1日 00:00:00 UTCからの経過秒数） 
- 標準ライブラリの datetime モジュールを使うことで、人が読める形式に変換できる
- ファイルの更新日時を取得する `Path.stat()`
- これを使ってSQLiteにファイル更新日を登録できるようにしよう
- Sparkを使っていくうえでの先輩からのアドバイス
- ①まず使い倒してみる
- ②使いながらどのくらいのメモリが必要かのさじ加減を掴む
- ③functionsの公式ドキュメントを眺める
- ④集計に grouping sets を使う
- ⑤内部的な動きを理解する（エラーなどの原因を追いやすくするため）
- LSPで処理を追う（LSP：Language Server Protocol）

## 8/25（火） 

- DB接続の切断やファイルのクローズを安全・簡潔に記述できる generator を使って記述できる
- yield を境界線にして、前後に処理を分けることができる
- yield を使うことで、for 文のときのようにデータを全てメモリに載せなくて済む
- yield を使うと「要求された時に1件ずつ作って渡す」ことができるため
- ジェネレータの概念はふわっと理解したので、帰宅してから実際に使ってみてジェネレータ関数を2つくらい作って試してみよう
- `contextlib.contextmanager` を使った実装に触れる

## 8/26（水）

- JupyterLabで .py を実行する方法としてランチャーからターミナルで実行した
- `shift + control + P` でインタープリタを選択するとカーネルが選択される
- Ruffとty を導入したことで、Pythonスクリプトの厳密性が増すことを教えていただいた
- これによって最新の規約に基づいた書き方を身に着けることができるらしい、ただかなり警告が多くて大変だと感じた
- 今後はルートを home ではなく、プロジェクト（スクリプト）ごとにする必要があるようになった（Ruffとtyを導入したことによって変更になった箇所）
- 今日一日、36協定の代表者選出の署名依頼の作成とチャット共有でほぼ半日が潰れた

## 8/27（木）

- git push する前までなら revert をすることは結構あるということを知った
- 初心者本で revert はなるべく使わないように解説されていたが、実際には使っているケースもあることを知れた
- `git init` を実行したときに起きていることは、そのフォルダの中に隠しディレクトリ .git が作成されるだけ
- Gitコマンドを実行すると、Gitは「現在のフォルダ（または親ディレクトリ）」内に .git というディレクトリが存在するかを探す。この .git ディレクトリの中にすべての変更履歴や設定データが保存されており、これの有無によってGit管理下かどうかを判断している
- Gitの仕組みは "キー・バリュー型のデータベース" とも言えるらしい
- プログラムのメモリ管理やデータ処理の方式は、大きく分けて全件オンメモリ処理とストリーミング（逐次・バッチ）処理の2つのタイプがある
- 自分が実装したのはオンメモリ処理だったため、メモリ消費が激しいことをアドバイスいただいた
- Pythonでリスト内包表記の `[ ]` を `( )` に変えるだけで、簡単にジェネレータを作成できる
- 引継ぎと署名集めに追われた1日だった