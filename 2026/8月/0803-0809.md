## 8/3(月) 休み

### 自学

- 新幹線移動中、Redshift格納前のバリデーション内容の精査対象の絞り込み
- 新規学習はしなかった


## 8/4(火)

### 自学

- XORの総和の考え方を習得

### 業務

- 先週欠席した会議の録画を視聴し、実現したいことによっては、ツールでの実現が難しいデータであることがわかった
- 処理速度を考慮して、CSVファイル内のデータ連結はVBAからPandasに変更していく。その仕組みについては、
- 現時点で4業務中2業務は置き換えが完了したので、残り2業務分を置き換えていく
- 同じCSVのデータを結合する処理でも、PandasとVBAで処理速度の違いがあるのは、VBAは毎回Excelのセルやオブジェクトにアクセスする往復コストとメモリ上の散らばったデータを探すアクセス遅延が生じるのに対し、Pandasは最初からデータをメモリの連続領域にまとめて載せ、一瞬で計算を終わらせる仕組みになっていることが関係していると知った
- VBAの処理速度を克服するために、メモリ上に一気に載せて処理すれば現在よりも速く処理できるようになるので、そこを改善していく
- SQLの内部的な動きのイメージとして、まず `CROSS JOIN`（直積）で、すべての行の組み合わせ（掛け算）を作り、そこから `ON` や `WHERE` の条件に合う行だけをフィルタリング（抽出）する→まず掛け算して、後から絞り込むイメージ
- RDBの設計において、多対多の関係になるときはそのままテーブルにせず、中間テーブルを挟んで 1 対 N に分解することが基本だということを知った。こうすることによって、重複が発生せず、すべての結合が「1 対 N」になり、SQLの JOIN が綺麗かつ高速に書けるようになるメリットがあるようだ
- 外国人向け消費税免税制度の変更に伴って計算ルールが変わることから、計算ロジックの見直しが一部必要になった。このように外部環境の変化によってこれまでのものが変化することを前提に、システムや処理をどのような将来的な変更に備えるか、という視点を持って取り組むようにしていきたい。また、新ルールを理解しないとアウトプットが変わってきてしまうため、その点も気をつけていきたい
- .xlsm から .csv に変換するスクリプトを作成したことで、ファイルを手動でCSV化する手間を削減
- .bashrc に alias を設定してターゲットとなる階層に一気に移動できるようにする方法を習得
- configには静的な設定を書くのであって、関数を呼び出した結果取得した値は動的なので適さないとわかった


### 自学

- ぶつかった課題: 与えられた整数を渡すと、1からnまでのXORの計算を行なう関数の実装において、与える整数が大きいとループ処理が膨大になり、時間がかかってしまった、または、計算が途中で終了し、エラーとなった
- 解決策: 連続する 4 数の組（例: 1,2,3,4）を XOR すると常に 0 になるという性質を使う
- 連続する 4 数の組 = 4k, 4k+1, 4k+2, 4k+3
- 4の倍数から始まることが最大のポイントで、4k からスタートすると必ず 0 になるのは、2進数で表したときに桁の上がり方がきれいに揃うから
- そこから +1, +2, +3 していった 4 つの数は、「下2桁だけが変化し、それより上の桁（4k の部分）は一切変化しない」 という状態になる
- 00, 01, 10, 11の繰り返し
- 「4 数の XOR が 0 になる」という性質を利用するときは、必ず 4 の倍数（4k）からスタートしているかが重要
- 逆にいうと、4の倍数からスタートしていないときは、法則に当てはまらない
- ただし、注意したいのは0からスタートする場合でも4の倍数として扱われる、つまり、k=0のときも連続する4数は排他的論理和の結果として 0 になる
- 同じ構造の隣り合う2数は XOR すると必ず 1 になる
- 直面した課題とその解決策をまとめておき、いつでも検索できる状態にしようと思った、過去の似たような場面があったときにすぐに引き出せるようにしよう

### 業務

- 昨日帰りがけに発生した、本来「1」として登録される必要があるカラムに対して、すべて「0」で登録されてしまっている課題を、数値の検証ロジックの前に対象のカラムの処理を配置することで解決した、原因は処理順だったとわかった
- 実態を見ること・定量的にどんな変化があるのかを見たうえで、価格を変更すること
- 「誰が」「いつまでに」「何を」やるかを明確にすること
- 「何がわかれば先に進めそうか」という仮説をもって、必要最小限の検索で済むように訓練が必要だと痛感
- 疑問のストックリストを作成し、調べるのは後に回すようにした
- `except` の後に指定するのは、エラーの「クラス（型）」で、オブジェクト（インスタンス）そのものではなく、エラーの種類を表す例外クラスを書く。 「どのような種類のエラークラスを捕まえるか」を指定し、必要に応じて as でその詳細なオブジェクトを取り出して利用する
- 通常処理の後から、追加でファイルのデータをRedshiftに格納するケースでは、これまではファイルを直接指定して取り出していたが、新規でディレクトリを作成して、そのディレクトリ内のファイルを直接読み取るようにすれば一回で済み、かつ、追加コードも少なくなるので、この対応方法に変更
- 対象のシリーズを関数に渡すと、後から処理内容を追加できるような仕様に変更した方がよさそうだと思ったので実装してみたら、使い勝手が良くなった
- 文字列型で設定したカラムに数値を文字列として格納する場合、後々の分析のためにカンマは排除した方がいいとわかった